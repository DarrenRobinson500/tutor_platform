{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "sleep 3 && python manage.py migrate && gunicorn main.wsgi:application --bind 0.0.0.0:$PORT"
  }
}